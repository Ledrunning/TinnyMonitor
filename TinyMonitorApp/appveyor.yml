image: Visual Studio 2022
configuration: Release
environment:
  major_version: 1
  minor_version: 7
  patch_version: 0
version: '$(major_version).$(minor_version).$(patch_version).{build}'
dotnet_csproj:
  patch: true
  file: 'TinyMonitorApp\TinyMonitorApp.csproj'
  package_version: '$(major_version).$(minor_version).$(patch_version)'
  assembly_version: '{version}'
  file_version: '{version}'
  informational_version: '{version}'
  
before_build: 
  # Display .NET Core version
  - cmd: dotnet --version
  # Display minimal restore text
  - cmd: dotnet restore
build_script:
  - cmd: dotnet build -c Release
artifacts:
- path: 'TinyMonitorApp\bin\Release\TinyMonitorApp.exe
  name: Nuget
 
 #C:\projects\tinnymonitor-lu1w9\TinyMonitorApp\TinyMonitorApp.csproj